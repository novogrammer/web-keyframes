{
  "name": "web-keyframes-example-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "dev": "npm run build && serve dist"
  },
  "devDependencies": {
    "sass": "^1.101.0",
    "serve": "^14.2.4",
    "web-keyframes": "file:../.."
  }
}
